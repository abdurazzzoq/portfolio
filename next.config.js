/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['cdn.hashnode.com', 'user-images.githubusercontent.com','mir-s3-cdn-cf.behance.net']
    }
}

module.exports = nextConfig
